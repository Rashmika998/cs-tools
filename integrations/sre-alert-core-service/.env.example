# Copy to .env and fill in real values.

# Cosmos DB for Apache Cassandra connection.
CASSANDRA_CONTACT_POINT="<account>.cassandra.cosmos.azure.com"
CASSANDRA_KEYSPACE="alertintegration"
CASSANDRA_KEY="<cosmos-db-primary-or-secondary-key>"
# Optional: defaults to the contact point's leading DNS label (Cosmos DB account name == username).
CASSANDRA_USERNAME=
# Optional: defaults to 10350.
CASSANDRA_PORT=10350

# Optional JSON of fallback values applied to any alert field left empty, e.g.
# '{"severity":"warning","category":"service_interruption"}'. Empty/unset disables defaulting.
CORE_ALERT_DEFAULTS=

# csm-integration-service (M2M, OAuth2 client credentials) -- the real incident API this service
# posts every new/updated incident to first. All required; the service refuses to start without them.
CSM_INTEGRATION_BASE_URL="https://<csm-integration-service-host>"
CSM_INTEGRATION_TOKEN_URL="<oauth2-token-endpoint>"
CSM_INTEGRATION_CLIENT_ID=
CSM_INTEGRATION_CLIENT_SECRET=
# Optional: comma-separated OAuth2 scopes.
CSM_INTEGRATION_SCOPES=

# CreateIncidentRequest.callerId -- the CSM caller uuid every incident this service creates is
# attributed to.
CSM_CALLER_ID=

# CreateIncidentRequest.serviceId fallback used when an alert's raw Service label has no match via
# a live /services/search lookup.
CSM_UNKNOWN_SERVICE_ID=

# Comma-separated Google Chat incoming-webhook URLs, used only while CSM hasn't confirmed an
# incident. Empty means an unconfirmed incident has nowhere to be seen until CSM recovers.
FALLBACK_CHAT_WEBHOOK_URLS=

# alert-core-service's HTTP port (/healthz, /livez, /alert). Optional: defaults to 8080.
PORT=8080
